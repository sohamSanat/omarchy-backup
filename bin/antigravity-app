#!/usr/bin/env bash
# Launcher script for Antigravity desktop application
APP_DIR="$HOME/.local/share/antigravity"
EXE="$APP_DIR/antigravity"

if [ ! -x "$EXE" ]; then
    echo "Antigravity executable not found at $EXE" >&2
    exit 1
fi

exec "$EXE" "$@"
