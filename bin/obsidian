#!/bin/bash
# Pre-launch check: dynamically sync Omarchy theme color palette to Obsidian vaults
if [[ -x "$HOME/.local/bin/omarchy-sync-obsidian" ]]; then
  "$HOME/.local/bin/omarchy-sync-obsidian" 2>/dev/null || true
fi

# Execute the main Obsidian binary
exec /usr/bin/obsidian "$@"
