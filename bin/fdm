#!/bin/bash
export QML_XHR_ALLOW_FILE_READ=1
if [[ -f "$HOME/.local/lib/libomarchy-fdm-theme.so" ]]; then
  export LD_PRELOAD="$HOME/.local/lib/libomarchy-fdm-theme.so${LD_PRELOAD:+:$LD_PRELOAD}"
fi
exec /home/soham/.local/share/freedownloadmanager/fdm "$@"
