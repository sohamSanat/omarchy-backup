# Omagen Demo

This directory is copied into the active Omagen session when Demo is opened.

It is intentionally deterministic so the editor, terminal and file manager
show useful, non-private content on every machine.
