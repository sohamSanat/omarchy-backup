#!/bin/bash

# Called when Omarchy unloads the bar widget after `omarchy plugin disable`.
# This stops the daemon without deleting the linked-device credentials.

source "$(dirname "$(readlink -f "$0")")/_common.sh"
wa_stop_service
