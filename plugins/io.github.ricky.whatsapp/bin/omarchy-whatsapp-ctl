#!/bin/bash

# omarchy:summary=Talk to the WhatsApp bridge daemon
# omarchy:args=<status|chats|refresh|messages|send|read|focus|pair|reconnect|logout|ping> [args]

source "$(dirname "$(readlink -f "$0")")/_common.sh"

exec "$(wa_node)" "$WA_DAEMON_DIR/ctl.js" "$@"
