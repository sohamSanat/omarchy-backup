#!/usr/bin/env python3
import sys
import os

BIN_DIR = os.path.dirname(os.path.abspath(__file__))
if BIN_DIR not in sys.path:
    sys.path.insert(0, BIN_DIR)

from stats_manager import main

if __name__ == "__main__":
    main()
